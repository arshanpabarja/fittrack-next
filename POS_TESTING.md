# کارتخوان باشگاه

نسخه جدید از درایور PEC پروژه قبلی در پوشه `Library` استفاده می‌کند.
اتصال پیش‌فرض واقعی و از نوع LAN، آدرس `192.168.100.54` و پورت `3030` است.
مبلغ برنامه تومان است؛ هنگام ارسال به دستگاه در ۱۰ ضرب و به ریال تبدیل می‌شود.
فقط پاسخ `00` همراه شماره پیگیری، پرداخت موفق محسوب می‌شود.

در تنظیمات مدیر می‌توان حالت واقعی/آزمایشی، IP، پورت و زمان انتظار را تغییر داد.
پس از ذخیره برنامه را ببندید و دوباره اجرا کنید. مقدار پیش‌فرض انتظار ۶۰ ثانیه است.
متغیر `FITTRACK_POS_MODE` در صورت وجود بر حالت ذخیره‌شده اولویت دارد؛
برای استفاده از تنظیمات صفحه، این متغیر را حذف کنید.
در حالت `fake` هشدار دائمی نمایش داده می‌شود؛ پرداخت‌ها بدون برداشت وجه ثبت می‌شوند.

وابستگی `pythonnet` در `requirements.txt` ثبت شده و DLLها داخل همین پروژه قرار دارند.
درایور از .NET Framework ویندوز استفاده می‌کند و به پوشه نسخه قدیمی وابسته نیست.
عدم بارگذاری درایور موجب خطا می‌شود و برنامه خودکار به پرداخت آزمایشی تغییر نمی‌کند.

ثبت‌نام حضوری، تکمیل ثبت‌نام سایت و تمدید، همگی از یک کارتخوان مشترک استفاده می‌کنند.
هنگام پرداخت روی دستگاه، تا نمایش نتیجه در برنامه منتظر بمانید.
در قطع ارتباط یا پایان زمان انتظار، نتیجه ممکن است نامشخص باشد؛
برنامه درخواست جدید را تا اجرای دوباره مسدود می‌کند. ابتدا رسید دستگاه و وضعیت
بانکی را بررسی کنید و پرداخت موفق را تطبیق دهید؛ راه‌اندازی دوباره به معنی برگشت وجه نیست.

تست‌های بدون برداشت وجه:

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests
```

برای تأیید نهایی در باشگاه، یک پرداخت موردنظر با کارت واقعی انجام دهید و مبلغ
ریالی روی دستگاه، نتیجه در برنامه و شماره پیگیری را با رسید تطبیق دهید.
